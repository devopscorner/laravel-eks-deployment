apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "svcrole.fullname" . }}-test-connection"
  labels:
    {{- include "svcrole.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "svcrole.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
